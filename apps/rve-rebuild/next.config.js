/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Disable telemetry
  experimental: {},
  // The rebuild does not require any external assets
};
module.exports = nextConfig;
